# Environment
.env
.env.local
.env.*.local

# Go relay build artifacts
relay/bin/
relay/tmp/
relay/server

# sqlc generated files — tracked in git as compile-time schema contract
# Regenerate with: make generate (or cd relay && sqlc generate)
# relay/internal/db/*.go  (intentionally not ignored)

# Frontend
web/node_modules/
web/.next/
web/.vercel/

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor
.vscode/
.idea/
*.swp
*.swo
