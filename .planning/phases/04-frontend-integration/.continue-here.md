---
phase: 04-frontend-integration
task: post-mvp-fixes
total_tasks: 5
status: paused
last_updated: 2026-03-23T14:37:31Z
---

<current_state>
MVP is live. Phases 1-3 complete (Go backend deployed on Easypanel). Frontend deployed on Vercel with live API data. Two agents successfully communicated through the relay tonight. Phase 4 was executed outside GSD plans because the original plans assumed a blank slate but user provided a polished 108-file frontend.

API: https://cnpj-explorer-quorum-api.62ickh.easypanel.host
Frontend: https://web-flowcoders.vercel.app
Deploy trigger: curl -sf -X POST "http://148.230.73.44:3000/api/deploy/5c71f2ac9dcdad5d7fdef922309625dab7637aceecd185ac"
</current_state>

<completed_work>

- Phase 1: Foundation — Go module, Docker Compose, DB schema (users/rooms/refresh_tokens/agent_presence), chi router, bearer auth, anonymous room creation, OAuth handlers, JWT sessions, rate limiting
- Phase 2: A2A Core — per-room hub goroutines, PresenceRegistry, HubManager, RoomExecutor, A2A-Version middleware, discovery endpoints (join/agents/info/heartbeat), presence reaper
- Phase 3: Streaming & Deploy — SSE keep-alive, X-Accel-Buffering middleware, Dockerfile (multi-stage), deployed to Easypanel with TLS
- Frontend wiring: provided frontend integrated with live API, explore page shows real rooms, room detail shows live agents + messages (SWR polling every 3-5s), login form wired to GitHub OAuth, dashboard page created, auth callback route created
- MVP fixes: direct JSON-RPC handler (bypassed a2a-go SDK), message polling endpoint (GET /r/{slug}/messages?after=N), MessageStore in-memory ring buffer, join endpoint fixed (registry.Add instead of hub.Subscribe), /stats endpoint, NEXT_PUBLIC_API_URL trailing newline fix
</completed_work>

<remaining_work>

1. **Fix agent_name in messages** — `relay/internal/relay/handler.go` line 126 stores `params.Message.Role` ("user") instead of actual sender name. Options: (a) look up agent name from bearer token hash in DB, (b) add optional sender_name field to message params, (c) extract from A2A message metadata
2. **Remove mock data from explore page** — `web/components/room-filters.tsx` has hardcoded categories ("All Rooms 1247", "Code Generation 342" etc.) and `web/components/room-grid.tsx` has 8 mock rooms as fallback + hardcoded "Showing 1-8 of 1,247" text and static pagination
3. **Wire Create Room on frontend** — explore page needs a "Create Room" button that POSTs to `/rooms` API. Dashboard's `CreateRoomDialog` component exists in `web/components/dashboard-content.tsx` but explore page doesn't have one
4. **OAuth login end-to-end** — `web/components/login-form.tsx` GitHub button redirects to `${apiUrl}/auth/github` but needs GitHub OAuth app credentials configured in Easypanel (GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET). Google needs the same.
5. **Hub broadcast channel buffered** — `relay/internal/hub/hub.go` line ~53: `broadcast: make(chan broadcastCmd)` is unbuffered, which blocks message/send when no hub goroutine is consuming. Change to `make(chan broadcastCmd, 64)` to allow fire-and-forget. Then re-enable broadcast in `relay/internal/relay/handler.go` (currently commented out at line ~129).
</remaining_work>

<decisions_made>

- Bypassed a2a-go SDK for message/send: SDK's NewJSONRPCHandler hung because it treats all requests as streaming. Wrote direct JSON-RPC handler that parses body, stores message, returns instantly.
- Skipped hub.Broadcast: unbuffered channel deadlocks when hub goroutine isn't active for the room. Messages stored in MessageStore for polling. Will re-enable after making channel buffered.
- Used user's frontend as-is: 108-file polished Next.js frontend with Navigation, Hero, Features, ExploreHero, RoomGrid, RoomDetail etc. Modified minimally — added SWR hooks, API fetch, types. Did NOT create components from scratch.
- Phase 4 plans are stale: original plans assumed blank slate. Actual work was done ad-hoc to wire existing components to the API. Plans should be marked as superseded or adapted.
</decisions_made>

<blockers>
None blocking. OAuth is optional for MVP (rooms can be created anonymously).
</blockers>

<context>
The user cares deeply about quality and gets frustrated when things are done without understanding what already exists. Always study existing code before modifying. No mocks, no stubs — everything must be real, production-ready. The user's frontend is polished and should not be overwritten or recreated. Test on prod after every deploy. The project uses pnpm for frontend, Go 1.26 for backend.

Key files to study before making changes:
- web/components/room-grid.tsx (explore page rooms grid)
- web/components/room-detail.tsx (room detail with live SWR data)
- web/components/room-filters.tsx (hardcoded categories to replace)
- relay/internal/relay/handler.go (message/send handler)
- relay/internal/hub/hub.go (hub with unbuffered broadcast channel)
- relay/cmd/server/main.go (full server wiring)
</context>

<next_action>
Start with: Fix agent_name in relay/internal/relay/handler.go. The `messages.Append(roomID, params.Message.Role, textContent)` on line 126 should use the actual agent name instead of the message role. The sender can be identified by looking up the bearer token from the Authorization header against the room's token_hash in the DB, then checking which agent is registered with that token. Or simpler: accept an optional `sender_name` field in the message params.
</next_action>
